<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- transport + wake -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Tier-1 capabilities (requested just-in-time at first use, not all at install) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- list/launch installed apps (Android 11+ package visibility) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- network.state (read-only) + alarm.set/timer.set (both normal, auto-granted) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:label="Agentic Android"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings: theme + which actions the agent may use. Launched from MainActivity's gear. -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- QR-scan pairing screen (Q5). Launched from MainActivity's "Pair agent" button. -->
        <activity
            android:name=".pairing.PairingActivity"
            android:exported="false" />

        <!-- device.wake: transparent activity that turns the screen on, shows over the keyguard,
             and dismisses an insecure lock. Finishes immediately; the Waker keeps the screen on. -->
        <activity
            android:name=".capabilities.WakeActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:exported="false" />

        <!-- Transparent biometric approval activity for `ask`-sensitivity capabilities (Q8). -->
        <activity
            android:name=".pairing.ConfirmerActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:exported="false" />

        <!-- Deny action on the approval notification. -->
        <receiver
            android:name=".pairing.ConfirmerDenyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.agenticandroid.CONFIRMER_DENY" />
            </intent-filter>
        </receiver>

        <!-- Always-on connection holder (Q2: persistent socket; FCM re-wakes it when Doze kills it). -->
        <service
            android:name=".PhoneAgentService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Always-on wake word (offline Vosk). Microphone foreground service. -->
        <service
            android:name=".WakeWordService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- Share agent-sent files out via the system share sheet (temp copies in cache/shared). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Restart the wake word after a reboot (if enabled + mic granted). -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FCM doorbell receiver. -->
        <service
            android:name=".WakeMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Notification access: lets notification.listen work and powers notifications.active /
             media.transport / OTP-and-SMS-text reads without the Play-restricted READ_SMS.
             User must also grant it in Settings > Notification access. -->
        <service
            android:name=".capabilities.AgentNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Tier 2: computer-use via Accessibility (tap/swipe/type/read/screenshot). -->
        <service
            android:name=".automation.AgentAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>
    </application>
</manifest>
